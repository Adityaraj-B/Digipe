import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Hero / dark section
  static const heroBackground   = Color(0xFF0D0D0D);
  static const heroDark         = Color(0xFF111111);

  // Brand
  static const sunOrange        = Color(0xFFF5A623);
  static const sunOrangeDeep    = Color(0xFFE8821A);
  static const sunGlow          = Color(0xFFFF6B00);

  // Stats positive green
  static const statGreen        = Color(0xFF22C55E);
  static const statGreenBg      = Color(0xFF16A34A);

  // Surface / bottom sheet
  static const surface          = Color(0xFFFFFFFF);
  static const surfaceLight     = Color(0xFFF8F8F8);

  // Text
  static const textPrimary      = Color(0xFF0D0D0D);
  static const textSecondary    = Color(0xFF6B7280);
  static const textHint         = Color(0xFFB0B7C3);
  static const textOnDark       = Color(0xFFFFFFFF);
  static const textOnDarkMuted  = Color(0xFFAAAAAA);

  // Input
  static const inputBorder      = Color(0xFFE5E7EB);
  static const inputFill        = Color(0xFFF9FAFB);

  // CTA button
  static const ctaButton        = Color(0xFF0D0D0D);
  static const ctaButtonText    = Color(0xFFFFFFFF);

  // Link / accent
  static const linkBlue         = Color(0xFF2563EB);

  // Card on dark
  static const cardDark         = Color(0xFF1E1E1E);
  static const cardDarkBorder   = Color(0xFF2A2A2A);
}
